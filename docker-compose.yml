services:
  postgres-db:
    container_name: postgres_fastai_container
    image: postgres:latest
    environment:
      POSTGRES_USER: h3kar360
      POSTGRES_PASSWORD: password
      POSTGRES_DB: fastai_db
      PGDATA: /var/lib/postgresql/data/pgdata
    volumes:
      - postgres-db:/var/lib/postgresql/data/pgdata
    ports:
      - "5430:5432"
    networks:
      - postgres-db
    restart: unless-stopped

networks:
  postgres-db:
    driver: bridge

volumes:
  postgres-db:
